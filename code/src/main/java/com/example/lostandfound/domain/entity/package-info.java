package com.example.lostandfound.domain.entity;

/**
 * Domain entity layer.
 * <p>
 * วาง JPA entity classes annotated ด้วย {@code @Entity} ที่นี่
 * Entities ควร extend คลาส {@code BaseEntity} เพื่อ inherit common audit fields
 * และควรใช้ Lombok annotations เพื่อลด boilerplate code
 * </p>
 */
