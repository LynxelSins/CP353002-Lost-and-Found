package com.example.lostandfound.mapper;

/**
 * Mapper layer.
 * <p>
 * Place mapping classes here to convert between Entity ↔ DTO.
 * วาง classes สำหรับ mapping ระหว่าง Entity ↔ DTO ที่นี่
 * objects พวกนี้ทำหน้าที่แปลงข้อมูลระหว่าง Entity และ DTO
 * ให้ใช้ static factory methods หรือ mapping library (เช่น MapStruct) เพื่อทำการ mapping
 * </p>
 */
